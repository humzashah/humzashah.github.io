# [humza.sh](https://humza.sh)

Using Jekyll to generate a static site that holds my CV.

Local setup:

```bash
# ensure you have the ruby version specified in Gemfile
bundle install --path vendor/bundle
bundle exec jekyll serve
```
